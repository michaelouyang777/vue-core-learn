/* @flow */

import { baseOptions } from './options'
// 引入src/compiler/index
import { createCompiler } from 'compiler/index'

const { compile, compileToFunctions } = createCompiler(baseOptions)

export { compile, compileToFunctions }
